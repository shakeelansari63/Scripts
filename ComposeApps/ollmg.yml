name: freak-ollama
networks:
  default:
    name: freak-ollama-net
services:
  ollama:
    extends:
      file: ollm.yml
      service: ollama
    devices:
      - nvidia.com/gpu=all

  open-webui:
    extends:
      file: ollm.yml
      service: open-webui
